# macOS 通知感知：手动阶段

本阶段新增 `ark.notifications` Skill、原生 AX 采集器、统一 Ark Event、本机 SQLite 记录和“智能总结”页面。微信是首个验收目标；采集、存储、模型接口均不依赖微信。

## 使用

1. 重启后端，运行 `./scripts/build_app.sh`，重新打开 `build/Ark Intelligence.app`。
2. 在 Skill 管理启用“通知感知与总结”。默认关闭。
3. 打开“智能总结”，点击“辅助功能授权”，在 macOS 系统设置中允许 **Ark Intelligence**。开发签名改变后可能需要移除旧条目并重新授权。
4. 选择最近 1 小时、24 小时、今天或自定义时间段；应用名称留空查询所有来源，填写时精确匹配采集到的名称。
5. 勾选本次采集许可，点击“智能总结”。Ark 尝试打开通知中心、回到列表顶部、自动展开可识别分组并滚动加载通知，全程无需逐张手动展开。采集期间请勿操作通知中心；系统不支持的操作会在结果中明确提示。
6. 可使用“总结已采集记录”重试摘要，无需再次访问其他应用；展开“核对原始通知”检查来源。清空采集库不会清除系统通知，也不会删除对话中先前引用的通知内容。

没有后台轮询、启动登录项、AXObserver、定时任务或自动回复。页面离开后已发起的摘要可以继续完成。

### 系统已允许但仍提示缺少辅助功能权限

先完全退出 Ark，再打开项目 `build/Ark Intelligence.app`。不能在旧进程运行时覆盖应用：进程仍使用旧代码，而系统设置中的条目可能已对应磁盘上的新版本。构建脚本现在会在构建前和替换应用前检查 Ark 是否已退出。

如果重开后仍未通过，进入系统设置 → 隐私与安全性 → 辅助功能，移除旧的 Ark 条目，再添加当前 `build/Ark Intelligence.app` 并允许。请使用同一路径启动，避免给另一个同名副本授权。不需要为 Python 后端授权。

默认 `codesign --sign -` 是临时签名，代码变化会改变其 designated requirement，macOS 无法稳定地跨版本复用授权；参见 [Apple TN3127](https://developer.apple.com/documentation/technotes/tn3127-inside-code-signing-requirements)。有固定开发签名证书时，可以通过 `ARK_CODESIGN_IDENTITY` 指定该证书构建。脚本不会创建证书、重置系统权限或绕过权限检查。

## 能力边界

这不是完整系统通知历史接口。Apple 的 [getDeliveredNotifications](https://developer.apple.com/documentation/usernotifications/unusernotificationcenter/getdeliverednotifications(completionhandler:)) 只获取调用应用自身的通知。跨应用采集通过 [AXUIElementCopyAttributeValue](https://developer.apple.com/documentation/applicationservices/1462085-axuielementcopyattributevalue) 读取界面；可用属性取决于目标程序和系统版本。

- 当前仅采集通知中心暴露的卡片，不读取微信聊天数据库、不抓取完整会话、不访问系统私有通知数据库。
- 已清除、未加载、隐藏预览、无法自动展开的分组内容可能缺失。不会点击消息、回复、关闭或清空通知。
- 使用通知 subrole 识别卡片，避免将天气等桌面组件误识别。macOS UI 标识没有稳定公开契约，未知结构会返回明确错误，不返回虚构通知。初始 subrole 兼容参考了 [PingPlace 的实测结构报告](https://github.com/NotWadeGrimridge/PingPlace/issues/44)；这不是 Ark 的实机验收结论。
- 每页最多扫描 2500 个节点、24 层；单次采集最多约 40 秒、100 轮操作、500 张卡片；AX 消息本身也有超时。标题最多 1000 字、正文最多 4000 字。达到限制时提示分批采集。
- 支持中文/英文的“刚刚、N 分钟前、N 小时前”等相对时间，精度标为 approximate。无法可靠识别的日期保留原始标签并标为 unknown；不会用 observed_at 补造接收时间。
- 范围为 `[start, end)`，要求显式时区，最多 31 天。unknown_time_count 统计所选区间内采集、但无法确认接收时间的记录；它们不参与接收时间摘要。
- 没有稳定源 ID 时使用内容和估计接收分钟去重；未知时间按采集日期去重。相同内容可能合并，相对时间重新估计也可能产生重复；不会将条数宣称为真实消息总数。
- 每次摘要最多 500 条；超出时拒绝并提示缩短区间，不静默丢弃。分批摘要保留每组输出，界面提供全部源事件用于核对。

## 代码与数据流

```text
用户手动操作
  → NotificationSummaryViewModel
  → ApplicationEventAdapter / NotificationCenterAXAdapter
  → 后台线程内自动展开/滚动 + 多轮 AX 遍历 + NotificationCardParser
  → POST /notifications/capture
  → ArkEvent 校验 → EventStore 去重持久化
  → POST /notifications/summary（时间与应用筛选）
  → 共享推理锁 + 本机 Ollama + 无工具摘要
  → 摘要、覆盖范围、源事件
```

聊天 Skill 使用同一采集器：`notifications.capture` 原生 prepare 只生成说明，用户确认后才读取；RunService 保存事件后只向模型回传计数。`notifications.query` 通过 PythonExecutor 查询同一数据库。聊天中的查询结果遵循现有任务日志与对话存储机制；独立摘要页面不写长期记忆或聊天记录。

统一事件字段：

| 字段 | 用途 |
| --- | --- |
| schema_version | 当前 `1.0` |
| kind | 当前 `notification.received`；保留 `application.state_changed` 类型供未来适配器使用 |
| adapter | 来源适配器，如 `macos.notification-center.ax` |
| source_app / source_bundle_id | 原始应用名与可选 bundle ID；未知来源不猜测 |
| source_id | 可选上游稳定 ID；通用 AX 角色标识不得作为消息 ID |
| title / body | 可读取通知内容 |
| occurred_at / observed_at | 可选接收时间 / 必填采集时间 |
| time_precision / time_label | exact、approximate、unknown，以及原始时间标签 |
| id | 存储层生成的去重摘要；不是 macOS 消息 ID |

通知数据库是 runtime 目录内 `notifications.sqlite3`（支持 `ARK_RUNTIME_DIR`），文件权限 0600。30 天窗口之外的记录不提供查询，在后续写入时清理；不为清理数据启动常驻任务。清空使用 SQLite secure_delete，系统备份、既有聊天历史不在清空范围内。

所有 HTTP 路由继承 Ark 的本地 bearer 校验和浏览器 Origin 拒绝策略；Hermes 的受限 token 不能访问这些路由。技能禁用时拒绝采集、查询与摘要；清空入口仍可使用。

独立摘要固定使用 `127.0.0.1:11434`，禁用环境代理，不继承 OLLAMA_HOST；发送通知前检查 `/api/show`，拒绝远端/云模型或无法确认的元数据。模型名称复用 `ARK_MAIN_MODEL` / Ark 当前模型。摘要客户端没有工具，也不调用记忆写入。单个摘要在 840 秒内完成，模型错误不回传可能包含通知内容的异常体；已采集数据保留供重试。

## 扩展

为其他社交应用实现 `ApplicationEventAdapter` 并返回相同事件，独立处理其 AX 布局和时间语义。模型和存储不需认识应用名称。当前未实现独立微信会话采集或应用状态变化检测。

未来常驻阶段可增设 AXObserver / NSWorkspace 通知驱动的采集调度，必须另外设计可见的监听开关、应用范围、暂停与退出、授权撤销、丢失/恢复边界；本次没有启用这些行为。不能把 AXObserver 的界面变化通知直接当成新消息，需在适配器中确认并去重。

## 验证

自动测试：

```bash
PYTHONPATH=backend:. backend/.venv/bin/python -m unittest discover -s backend/tests
swift test --package-path frontend --scratch-path /private/tmp/ark-notification-tests -j 2
backend/.venv/bin/python scripts/notification_summary_smoke_test.py
```

smoke test 只把脚本内的合成通知发给本机模型，不读真实通知。单元测试覆盖来源隔离、时间边界、未知时间、幂等去重、清理、技能禁用、模型失败保留数据、云模型拒绝、分批覆盖、通知分组和桌面组件排除。

实机验收清单（需授权后执行，目前尚未全部通过）：

1. 微信收到一条有已知内容/时间的通知；通知预览开启，保持分组折叠，点击一次智能总结，核对来源、标题、正文和时间精度。
2. 微信多个会话的折叠/展开分组、另一社交应用同时来消息；确认各条不跨应用混淆，不将分组整体当多条完整历史。
3. 再次采集验证重复处理；使用跨午夜、自定义时区、边界时间筛选，核对相对时间估计误差。
4. 清除系统通知后重采集，确认无法补回未保存通知；隐藏预览和未知时间须明确显示限制。
5. 拒绝/撤销辅助功能、原生宿主断开、Ollama 离线、关闭 Skill；确认报错与已有记录保留行为。
6. 页面打开后不点击采集，检查没有新增记录；结束 Ark 后无采集进程或登录项。

本次验证记录：后端全套 65 项（3 项既有跳过）；Swift 测试与构建通过；合成通知已成功调用本机 `qwen3.5:9b-mlx`。自动展开版本新增了分组与单条消息区分、无变化停止、多页去重和部分失败保留测试。真实微信通知采集及各 macOS 布局仍待上述验收。

### 自动展开与滚动实现

`NotificationSweep` 使用可测试的采集驱动，每次执行一个操作后重新读取 AX 树，禁止缓存坐标和继续使用旧节点。仅对确认的折叠栈（系统 subrole 及 AXPress / Show Details / Clear All 三项操作组合）执行展开点击，或使用明确未展开的 disclosure 控件。单条消息、回复、清除和内容中名为“展开”的普通文本不会触发操作。这些系统操作特征参考 [macos-notification-cli 的结构记录](https://github.com/coryfklein/macos-notification-cli/blob/main/DESIGN.md)，也兼容实机观察到的中文 iPhone 镜像分组：通知列表中的 AXGroup，描述以“，已叠放，来自iPhone”或“，已叠放”结尾，提供 AXPress、显示详细信息、全部清除且不提供关闭；允许额外回复等操作，但不会执行它们。操作名称兼容 Target 后缀分隔空格。

滚动限定于包含通知列表的 AXScrollArea，使用暴露的按页向下操作或其垂直滚动条的增量操作。达到无变化、节点/时间/步数上限时结束，返回覆盖提示。开始时尝试将垂直滚动条定位到顶部；不会关闭或恢复分组，以免再次触发内容相关操作。失败分组的折叠预览不冒充完整消息，其他已读取内容保留。仍然是手动触发，不注册后台监听。

2026-09-23 实机复核：重新授权并重启后成功采集 24 条并完成本地总结；发现旧规则未展开镜像叠放卡。通过通知中心点击验证分组会展开成多条卡片，已据此修正识别规则并通过 26 项 Swift 测试。修正版自动展开仍需构建后实测。

2026-09-23 诊断确认：当前系统返回 AXNotificationCenterBannerStack，命名操作包含换行分隔的 Target / Selector 元数据。已支持换行归一化及 BannerStack（允许额外操作但拒绝带关闭动作的单条卡）。滚动区域重新校验不再比较会变化的整棵通知文本树，仍校验区域类型、标识和通知范围；展开目标保持完整快照比较。28 项 Swift 测试通过，含实机操作名称回归；新版实机展开结果尚待验证。诊断文件 notification-ax-structure.json 位于 Ark runtime，覆盖保存控件结构，不保存通知标题或正文。

2026-09-23 19:00 实机验收：重启已授权修正版后，从前端点击一次智能总结，Ark 返回自动展开 15 次，采集 114 条，新增 69 条，83 条接收时间不明。滚动 0 次，系统操作未成功继续；因此确认分组自动展开与采集有效，不将此次结果描述为全部历史通知或跨页滚动验收通过。本地总结在采集后启动。

同次验收的本地总结已完成并在前端显示：qwen3.5:9b-mlx 对所选区间的 48 条库内通知分组总结。此数量包含此前已采集记录，不等于本次新增数量；接收时间不明的记录按现有规则排除。

滚动修正：AX 按页操作失败或成功但位置未变时，刷新目标后尝试垂直滚动条 AXIncrement。仅滚动条数值或同一通知位置变化才计数，不以全文变化作为位移证据。仅显式滚动条末端值确认到底；无位移/无后备操作时报告可能漏采，并显示目标校验或 AX 执行错误码。无法定位顶部也单独提示。32 项 Swift 测试通过，新增失败后备、无位移后备、文本不变位置改变、明确底部终止回归；新版仍需实机验收。

滚动实机诊断：展开 16 次，读取 122 条；AXScrollDownByPage 返回 -25204 (kAXErrorCannotComplete)，通知区域无 AXVerticalScrollBar，后备失败。不能据此认定到底。操作执行的 AX 超时已从继承的 0.1 秒调整为 2 秒（属性快照仍为 0.1 秒），待授权后排除超时因素；不将该调整描述为已修复实机滚动。

无滚动条方案：读取 AXScrollArea 的 AXPosition/AXSize 并验证进程属于 NotificationCenter；在区域中心创建像素滚轮事件，通过 CGEvent.postToPid 定向发送。向下优先滚轮，其次 AX 翻页/滚动条；向上定位最多 20 步，整体仍受 40 秒上限约束。每次不超过视口 60% 或 360 像素。发出事件不代表滚动成功，仍核验相同通知卡的坐标变化；没有顶部/底部位置证据时不宣称完整。34 项测试通过，定向事件实际送达效果待实机验证。

2026-09-24 实测：定向 postToPid 滚轮两次均未观察到位移（第二次全程 45 秒不切换窗口）；第一轮展开 17 次、读取 118 条并完成本地总结。普通桌面滚动对照可见从 QQ/淘宝区域移动到网易云音乐/高德区域。改用可见卡片与视口交集中心，经系统 AX hit-test 确认归属 NotificationCenter 后，以 HID 鼠标移动、滚轮、恢复鼠标位置的事件序列发送。不会点击卡片。35 项测试通过；新版系统滚轮仍待授权后验收。

2026-09-24 10:19 用户实测截图确认 HID 方案有效：自动展开 16 次、滚动 25 次、读取 130 条、新增 14 条；最后未观察到位移不等于此前没有滚动，也不构成到达底部的证据。

时间解析补充：支持本地日历的今天/昨天/前天加时钟、当天时钟及中英文相对分钟/小时、完整日期加时钟和月日加时钟。镜像卡标题中的末尾时间需由独立 AXDescription 佐证其不属于消息正文才提取；独立正文和标题中的时钟不作为接收时间。仅有“昨天”“2天前”仍缺少时刻，不伪造时间纳入精确区间。摘要先判断相关性，过滤推广、泛资讯和无关闲聊，保留具体沟通、待办、账单与安全提醒；原始候选记录仍可核对。

验证：37 项 Swift 测试及 13 项通知后端测试通过。本机 qwen3.5:9b-mlx 合成混合样例实测保留工作安排、陌生设备登录和账单共 3 条，过滤广告、娱乐资讯、无关群聊和提示词注入共 4 条；全噪声样例返回无相关消息。该小样本不代表所有通知的过滤准确率。

同日真实记录摘要复测：选取截图对应 09:15–10:15 的 39 条候选通知，第一版提示词仍保留免费试用与无关群聊，因此进一步加入明确反例及个人关联要求。最终本机复测返回无需要关注的相关消息；混合合成样例中 3 条重要消息仍全部保留、4 条无关内容全部过滤。此结果说明该批样例的过滤改善，不代表泛化准确率已验证。最终规则已加载到运行中的后端。

2026-09-24 重新授权后的现场测试：自动展开 20 次、滚动 17 次，采集 158 条，其中 122 条成功解析接收时间、36 条缺少时间标签。直接检查本次时间标签确认“昨天 20:40”和当天“09:16”等格式已生效；不能用不同时刻的两批通知计算严格准确率。仍无可靠底部证据。

摘要复测发现“整批全部无关”会漏掉流量用尽收费和群内面试变更，单纯缩小批次不足以修正。改为最多 8 条、约 2200 字符一批逐条返回 JSON 判断；使用请求内短编号，程序映射原始 ID，并拒绝漏项、重复项及未知编号。仅保留相关摘要，空判断不展示。增加窄范围的明确推广/泛资讯规则，不按整个来源应用屏蔽：免费试用次数促销、功能速览、一般放假购票推送、缺少地点的截断地震资讯、自愿娱乐接龙。实际本地灾害预警、服务风险、账单及安排变更仍可交模型判断。群聊内容需保留适用条件，发送者不等于办理者。

可运行 `backend/.venv/bin/python scripts/eval_notification_summary.py` 对本机 qwen3.5:9b-mlx 执行合成回归（不含真实用户数据）：5 条重要提醒保留、4 条无关内容过滤、全噪声返回空摘要提示。真实通知筛选仍依赖本地模型判断，原始记录保留供核对。

最终前端验收：同一最近 24 小时时间窗的 190 条候选记录，经最终后端规则与逐条模型判断，前端显示 3 条提醒（两条面试安排、一条共享流量用尽计费风险）；未出现免费试用广告、泛新闻、娱乐接龙。17 项通知后端测试通过，Swift 采集实现本轮未重建，现有辅助功能授权保持有效。窗口中 178 条时间不明统计包含先前采集记录，不等于本次采集的 36 条。
